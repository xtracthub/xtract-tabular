import unittest

"""
        STEP 1. Pass the unittests in tests.py. This is the first step to having an allowable structured extractor.
"""
from tests import ExtractionTests
unittest.main()
# If this passes -- CONGRATS -- you'll automatically move to Step 2!

""" 
        STEP 2. Benchmark the performance on three large files, themed as follows: 
        
        File A: A file with many rows, but only a couple columns.
        File B: A file with only a couple rows, but many columns. 
        File C: File A, but with a VERY LONG freetext header.  
"""


